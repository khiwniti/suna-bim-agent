---
phase: 07-element-selection-property-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx
  - apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx
  - apps/frontend/src/components/thread/carbon-bim-computer/BIMPanel.tsx
  - .planning/ROADMAP.md
autonomous: true
requirements: [SEL-01, SEL-02, SEL-03, SEL-04]
must_haves:
  truths:
    - "ElementPropertyPanel component exists with correct TypeScript interfaces (ElementProps, PropertySet)"
    - "BIMViewer click handler extracts expressId and calls extractElementProps"
    - "BIMViewer renders ElementPropertyPanel overlay when selectedProps is non-null"
    - "BIMPanel renders BIMViewer (onElementSelect prop is optional and accepted)"
    - "TypeScript compiles the affected files without new errors"
  artifacts:
    - path: "apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx"
      provides: "Property panel UI with collapsible sections"
      exports: ["ElementProps", "PropertySet", "default (ElementPropertyPanel)"]
    - path: "apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx"
      provides: "3D viewer with click-to-select and property extraction"
      contains: "extractElementProps"
  key_links:
    - from: "BIMViewer.tsx"
      to: "ElementPropertyPanel.tsx"
      via: "import ElementPropertyPanel, { type ElementProps }"
      pattern: "import ElementPropertyPanel.*from.*ElementPropertyPanel"
    - from: "BIMViewer.tsx"
      to: "OBC Highlighter events"
      via: "highlighter.events.select.onHighlight → extractElementProps → setSelectedProps"
      pattern: "highlighter\\.events\\.select\\.onHighlight"
    - from: "BIMPanel.tsx"
      to: "BIMViewer.tsx"
      via: "lazy(() => import('./BIMViewer'))"
      pattern: "BIMViewer"
---

<objective>
Verify the existing Phase 7 implementation — element selection and property panel — is correct, complete, and compiles cleanly. Then update ROADMAP.md to reflect completion.

Purpose: Phase 7 was implemented in a prior session. This plan validates that everything is wired correctly before marking it done.
Output: Verification report confirming implementation, updated ROADMAP.md
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-element-selection-property-panel/07-CONTEXT.md
@apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx
@apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx
@apps/frontend/src/components/thread/carbon-bim-computer/BIMPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify element selection & property panel implementation</name>
  <files>
    apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx
    apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx
    apps/frontend/src/components/thread/carbon-bim-computer/BIMPanel.tsx
  </files>
  <action>
Verify all Phase 7 requirements are implemented correctly by running these checks:

1. **ElementPropertyPanel exports** — Confirm the file exports:
   - `ElementProps` interface with fields: expressId (number), type (string), name (string|null), globalId (string|null), storey (string|null), material (string|null), quantities (Record), propertySets (PropertySet[])
   - `PropertySet` interface with psetName and properties array
   - Default export `ElementPropertyPanel` component accepting `{ props: ElementProps; onClose: () => void }`
   - Run: `grep -n "export interface\|export default" apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx`

2. **BIMViewer click handler** — Confirm:
   - `extractElementProps` async function exists, accepts (expressId, model, comps, OBCMod)
   - Highlighter "select" event calls `extractElementProps` → `setSelectedProps`
   - Deselection wired via `highlighter.events.select.onClear` → `setSelectedProps(null)`
   - Run: `grep -n "extractElementProps\|onHighlight\|onClear\|setSelectedProps" apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx`

3. **BIMViewer renders panel** — Confirm:
   - `selectedProps` state: `useState<ElementProps | null>(null)`
   - Conditional render: `{selectedProps && <ElementPropertyPanel props={selectedProps} onClose={...} />}`
   - Run: `grep -n "selectedProps\|ElementPropertyPanel" apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx`

4. **BIMPanel → BIMViewer wiring** — Confirm:
   - BIMPanel lazy-imports BIMViewer
   - BIMViewer accepts optional `onElementSelect` prop in its interface
   - Run: `grep -n "BIMViewer\|onElementSelect" apps/frontend/src/components/thread/carbon-bim-computer/BIMPanel.tsx apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx`

5. **TypeScript compilation check** — Run:
   ```bash
   cd apps/frontend && npx tsc --noEmit 2>&1 | head -50
   ```
   Document results. Distinguish pre-existing errors (from other files) vs new errors in Phase 7 files. Phase 7 files are:
   - `components/ElementPropertyPanel.tsx`
   - `BIMViewer.tsx`
   - `BIMPanel.tsx`
   
   If there are errors ONLY in Phase 7 files, fix them. If errors are all in other files, document as pre-existing and proceed.

6. **Collapsible sections** — Confirm ElementPropertyPanel has:
   - CollapsibleSection sub-component with defaultOpen prop
   - Quantities section with `defaultOpen` (open by default per context decisions)
   - Property sets rendered with CollapsibleSection (collapsed by default)
   - Run: `grep -n "CollapsibleSection\|defaultOpen" apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx`
  </action>
  <verify>
    <automated>cd apps/frontend && grep -c "export interface ElementProps" src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx && grep -c "extractElementProps" src/components/thread/carbon-bim-computer/BIMViewer.tsx && grep -c "selectedProps" src/components/thread/carbon-bim-computer/BIMViewer.tsx && grep -c "ElementPropertyPanel" src/components/thread/carbon-bim-computer/BIMViewer.tsx && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - ElementPropertyPanel.tsx exports ElementProps, PropertySet interfaces and default component
    - BIMViewer.tsx has extractElementProps function wired to Highlighter select events
    - BIMViewer.tsx conditionally renders ElementPropertyPanel when selectedProps is set
    - BIMPanel.tsx lazy-loads BIMViewer correctly
    - TypeScript compilation results documented (Phase 7 files compile or pre-existing errors noted)
    - CollapsibleSection pattern confirmed with correct defaultOpen settings
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md to mark Phase 7 complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update the Phase 7 section in `.planning/ROADMAP.md`:

1. Change the Phase 7 entry from:
   ```
   ### Phase 7: Element selection & property panel
   
   **Goal:** Click/hover element selection in 3D view triggers a property panel showing IFC metadata, material, and cost breakdown
   **Depends on:** Phase 6
   **Plans:** 0 plans
   
   Plans:
   - [ ] TBD (run /gsd-plan-phase 7 to break down)
   ```
   To:
   ```
   ### Phase 7: Element selection & property panel
   
   **Goal:** Click/hover element selection in 3D view triggers a property panel showing IFC metadata, material, and cost breakdown
   **Depends on:** Phase 6
   **Plans:** 1/1 plans complete
   
   Plans:
   - [x] 07-01-PLAN.md — Verify element selection & property panel implementation (completed)
   ```

2. Update the Milestone 2 progress section to mark Phase 7 complete:
   Change `- [ ] Phase 7: Element selection & property panel` to `- [x] Phase 7: Element selection & property panel`

3. Commit the changes:
   ```bash
   cd /teamspace/studios/this_studio/suna-bim-agent
   git add .planning/ROADMAP.md .planning/phases/07-element-selection-property-panel/07-01-PLAN.md
   git commit -m "docs(07): verify element selection & property panel implementation, update roadmap"
   ```
  </action>
  <verify>
    <automated>grep -A3 "Phase 7:" .planning/ROADMAP.md | grep -c "1/1 plans complete" && grep "\\[x\\] Phase 7" .planning/ROADMAP.md | head -1 && echo "ROADMAP UPDATED"</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 7 section shows "1/1 plans complete" with plan listed as done
    - Milestone 2 progress shows Phase 7 checked off
    - Changes committed to git
  </done>
</task>

</tasks>

<verification>
Run all verification commands in sequence:

```bash
# 1. Confirm ElementPropertyPanel exports
grep "export interface ElementProps" apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx
grep "export interface PropertySet" apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx
grep "export default function ElementPropertyPanel" apps/frontend/src/components/thread/carbon-bim-computer/components/ElementPropertyPanel.tsx

# 2. Confirm BIMViewer selection wiring
grep "extractElementProps" apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx
grep "setSelectedProps" apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx
grep "ElementPropertyPanel" apps/frontend/src/components/thread/carbon-bim-computer/BIMViewer.tsx

# 3. Confirm BIMPanel lazy import
grep "BIMViewer" apps/frontend/src/components/thread/carbon-bim-computer/BIMPanel.tsx

# 4. TypeScript check (document output)
cd apps/frontend && npx tsc --noEmit 2>&1 | head -30

# 5. Confirm ROADMAP updated
grep -A5 "Phase 7" .planning/ROADMAP.md
```
</verification>

<success_criteria>
- [ ] ElementPropertyPanel.tsx exports ElementProps, PropertySet, and default component — confirmed via grep
- [ ] BIMViewer.tsx has click handler → extractElementProps → setSelectedProps pipeline — confirmed via grep
- [ ] BIMViewer.tsx conditionally renders ElementPropertyPanel overlay — confirmed via grep
- [ ] BIMPanel.tsx lazy-loads BIMViewer (onElementSelect is optional prop, may or may not be passed) — confirmed via grep
- [ ] TypeScript compilation results documented (new Phase 7 errors = 0, pre-existing errors noted separately)
- [ ] ROADMAP.md updated with Phase 7 marked complete
- [ ] All changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/07-element-selection-property-panel/07-01-SUMMARY.md`
</output>
